extends layout.pug
block content
    div(
        id='editor'
        contenteditable='true'
        class='editor'
    ) !{content}
    button(
        id='submit'
    ) submit
    script(src="https://cdn.bootcdn.net/ajax/libs/axios/1.3.6/axios.min.js") 
    script(src="https://cdn.bootcdn.net/ajax/libs/marked/4.3.0/marked.min.js") 
    script(src="/js/editor.js") 

    div(id='board')

    div(
        id="test"
        contenteditable='true'
    ) 
      
